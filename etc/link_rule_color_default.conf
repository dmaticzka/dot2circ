<rule>
    # by default, use grey
    condition = 1
    # set color of palette
    # examples for palettes: http://mkweb.bcgsc.ca/brewer/swatches/brewer-palettes-swatches.pdf
    color = eval(sprintf("%s-%s","conf(.,link_color_default)",var(color)))
</rule>