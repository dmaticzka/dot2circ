<rule>
    # always trigger this rule
    condition  = 1
    # add _a1 to the color of the ribbon giving it transparency
    color      = eval(sprintf("%s_%s",var(color), "conf(.,link_transparency_level)"))
    flow = continue
</rule>